# Учебный проект: Blog (PHP + MySQL)

Аккуратный учебный блог с регистрацией, авторизацией, лентой постов, AJAX-комментариями и админ-панелью.

## Что реализовано
- Регистрация, вход, выход (сессии, валидация, хеширование паролей).
- Лента постов карточками, сортировка по дате, пагинация по 5 элементов.
- Полная страница поста + комментарии.
- Добавление комментариев через AJAX (`fetch`) без перезагрузки.
- Админ-панель для `admin`: CRUD постов, загрузка изображений, удаление комментариев.
- Безопасность: PDO prepared statements, экранирование HTML, CSRF-токены.
- Адаптивный современный интерфейс (телефон, планшет, десктоп).

## Быстрый старт (локально)
1. Создайте БД и импортируйте `schema.sql` (см. подробную инструкцию в `docs/DB_SETUP.md`).
2. Настройте БД через переменные окружения (`DB_HOST`, `DB_NAME`, `DB_USER`, `DB_PASS`) или оставьте дефолтные значения из `config/db.php`.
3. Запустите PHP-сервер:
   ```bash
   php -S 0.0.0.0:8080
   ```
4. Откройте: `http://localhost:8080/index.php`

## Документация
- Полная структура проекта: `docs/PROJECT_STRUCTURE.md`
- Полный SQL: `schema.sql`
- Настройка БД (пошагово): `docs/DB_SETUP.md`
- Запуск на XAMPP: `docs/XAMPP_SETUP.md`
- Деплой на Beget: `docs/BEGET_DEPLOY.md`
